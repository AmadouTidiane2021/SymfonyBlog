{% extends 'base.html.twig' %}

{% block title %}
{{ (formName) }}

Formulaire
{% endblock %}

{% block body%}
	<div style="max-width: 600px; margin:auto;">
		<h1>Formulaire {{ formName }}</h1>
			{{ form(dataForm) }}
	</div>
{% endblock %}
